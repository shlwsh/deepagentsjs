---
description: 自动总结代码变更并提交到远程仓库
---

# Git 自动提交工作流

这个工作流会自动完成以下操作:
1. 检测 Git 仓库的所有变更
2. 使用 AI 分析代码变更并生成提交信息
3. 提交代码到本地仓库
4. 推送到远程仓库

## 使用方法

```bash
bun run mygit
```

## 工作流程

1. **检查 Git 仓库** - 确认当前目录是 Git 仓库
2. **检测变更** - 获取所有修改、新增、删除的文件
3. **添加到暂存区** - 执行 `git add .`
4. **生成提交信息** - 使用 LLM 分析代码差异,生成规范的提交信息
5. **提交代码** - 执行 `git commit`
6. **推送到远程** - 执行 `git push`

## 注意事项

- 确保已配置 Git 用户信息(`git config user.name` 和 `git config user.email`)
- 确保有远程仓库的推送权限
- 生成的提交信息会使用中文,并遵循常见的提交规范
- 日志会记录到 `logs/app.log` 文件中

## 示例输出

```
📝 检测到以下变更:
  修改: src/index.ts, src/config.ts
  新增: scripts/git-auto-commit.ts

🤖 正在使用 AI 生成提交信息...

📋 生成的提交信息:
──────────────────────────────────────────────────
feat: 添加 Git 自动提交工具

- 创建 git-utils.ts 提供 Git 操作封装
- 实现 AI 自动生成提交信息功能
- 支持自动推送到远程仓库
──────────────────────────────────────────────────

✅ 代码已提交到本地仓库
🚀 正在推送到远程仓库 origin...
✅ 代码已推送到远程仓库
```
